# Cons-istant

Premium corporate scheduling UI mock with a Cal.com-style booking flow, dark navy + warm gold visual system, and Paddle payment step.

## Run

Open `/home/runner/work/Cons-istant/Cons-istant/index.html` in a browser.
Minimal static site setup for Vercel deployment.

## Deploy on Vercel

1. Push this repository to GitHub.
2. In Vercel, click **Add New Project** and import this repository.
3. Keep the default settings (no build command is required).
4. Deploy.

`vercel.json` is included to:
- serve clean URLs
- route all paths to `index.html` (SPA-friendly routing)
